# 3.5 实物日志开关方式和绘图方式

## 用途

本节说明实物运行时如何开启日志、定位日志文件，并使用 CSV 绘图工具分析数据。

## 控制器日志

`config.yaml` 中的控制器日志开关：

```yaml
logging:
  enable_logging: true
  log_file_path: "log/robot_log.txt"
```

查看日志：

```bash
tail -n 80 log/robot_log.txt
tail -f log/robot_log.txt
```

## RL 数据日志

`RL_WALK` 日志：

```yaml
rl_walk:
  enable_logging: false
  log_file_path: "log/log_RL_data.csv"
```

`RL_RUN` 日志：

```yaml
rl_run:
  enable_logging: false
  log_file_path: "log/log_RL_run_data.csv"
```

需要分析策略输入、输出或动作趋势时，可以临时打开对应开关并重新启动控制器。

## 电机跟踪日志

`config.yaml` 中的电机跟踪日志：

```yaml
robot_runner:
  enable_motor_trace_logging: false
  motor_trace_log_path: "log/motor_trace.csv"
```
无参数时默认读取脚本内置路径（通常为 log/log_RL_data.csv / log/motor_trace.csv），
该日志用于分析关节期望位置、实际位置、速度和力矩等数据。

## 绘图方式

普通 CSV / RL 数据：

```bash
python3 scripts/data_viewer.py
python3 scripts/data_viewer.py log/log_RL_data.csv
```

电机跟踪数据：

```bash
python3 scripts/motor_trace_viewer.py
python3 scripts/motor_trace_viewer.py log/motor_trace.csv
```

## 成功标志

- `log/` 下生成对应日志文件。
- CSV 文件有表头和持续更新的数据行。
- 绘图工具可以正常打开文件。
- 曲线中没有明显 NaN、Inf、异常尖峰或长时间断点。

## 安全提示

实物运行时不要长期打开过多高频日志，避免影响磁盘空间和实时性。调试完成后建议关闭临时 CSV 日志开关。
